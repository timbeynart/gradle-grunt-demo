/**
 * This object doesn;t really do anything.
 * @constructor
 */
function World() {
  /**
   * Thie method takes two arguments, converts them to integers, and returns the sum of the two integers.
   * @param num1 {number}
   * @param num2 {number}
   * @returns {int} The sum of the two arguments
   */
  this.peace = function (num1, num2) {
    return parseInt(num1) + parseInt(num2);
  };
}
